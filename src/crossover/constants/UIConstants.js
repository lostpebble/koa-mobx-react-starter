// N.B. All constant string values must be
// unique to avoid clashes in the language
// dictionary. Hence "TAB_COUNTER" instead
// of just "COUNTER"

export const CTabs = {
	USER: "TAB_USER_PROFILE",
	COUNTER: "TAB_COUNTER",
};
