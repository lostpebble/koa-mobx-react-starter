// N.B. All constant string values must be
// unique to avoid clashes in other parts of
// the app, such as a language dictionary.
// Hence "TAB_COUNTER" instead of just "COUNTER"

export const CTabs = {
  USER: "TAB_USER_PROFILE",
  COUNTER: "TAB_COUNTER",
};
